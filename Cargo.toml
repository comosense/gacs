[package]
name = "gacs"
version = "0.7.1"
edition = "2024"

[dependencies]
sha2 = { version = "0.11.0", default-features = false }
thiserror = { version = "2.0.18", default-features = false }

[profile.release]
codegen-units = 1
lto = true
opt-level = 'z'
panic = 'abort'
strip = true
